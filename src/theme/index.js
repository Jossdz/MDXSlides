import theme from 'mdx-deck/themes'

export default {
  // extends the default theme
  ...theme,
  // add a custom font
  font: 'Poppins, Roboto, sans-serif',
  // custom colors
  colors: {
    text: 'white',
    background: '#011627',
    link: '#56555c',
  }
}